# Mark the H_starts and H_stops points
            # ax.scatter(H_starts, actual_T[500] * len(H_starts), color='blue', marker='o', label='H_starts')
            # ax.scatter(H_stops, actual_T[500] * len(H_stops), color='red', marker='x', label='H_stops')

            # # Annotate the start and stop points
            # for i, (start, stop) in enumerate(zip(H_starts, H_stops)):
            #     ax.text(start, actual_T[500], f'Start {i+1}', ha='right', color='blue')
            #     ax.text(stop, actual_T[500], f'Stop {i+1}', ha='left', color='red')
